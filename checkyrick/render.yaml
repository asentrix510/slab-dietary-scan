services:
  - type: web
    name: checkyrick-api
    runtime: node
    plan: free
    rootDir: checkyrick/backend
    buildCommand: npm install && npm install -g @agentrhq/webcmd
    startCommand: node server.js
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: NODE_VERSION
        value: 20.18.0
